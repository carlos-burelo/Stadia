export interface AnimeI {
    title?: string,
    banner?: string,
    description?: string,
    status?: string,
    date?: string,
    img?: string,
    genders?: any,
    sugestions?: any,
    episodes?: any,
}