export interface animeI {
    title?: string,
    description?: string,
    status?: string,
    date?: string,
    img?: string,
    id?: string,
    genders?: object,
    sugestions?: object,
    episodes?: Number,
    success?: boolean
}